#ifndef GLOBAL_H
#define GLOBAL_H
#include <QtCore>
#include <iostream>

static QTextStream qout(stdout, QIODevice::WriteOnly);
static QTextStream qin(stdin, QIODevice::ReadOnly);

#endif // GLOBAL_H
